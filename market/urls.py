from django.urls import path
from rest_framework import routers

from .views import *



# router = routers.DefaultRouter()

# router.register(r'user', UserViewSet, basename='user-detail')
# router.register(r'store-detail', StoreViewSet, basename='store-detail')
# router.register(r'store-list', StoreListViewSet, basename='store-list')
# router.register(r'category-detail', CategoryViewSet, basename='category-detail')
# router.register(r'category-list', CategoryListViewSet, basename='category-list')
# router.register(r'product-detail', ProductViewSet, basename='product-detail')
# router.register(r'product-list', ProductListViewSet, basename='product-list')


# urlpatterns = router.urls